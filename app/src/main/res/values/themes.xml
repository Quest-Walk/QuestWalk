<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QuestWalk" parent="@android:style/Theme.Material.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.QuestWalk" parent="Base.Theme.QuestWalk" />

    <!-- Splash screen theme -->
    <style name="Theme.QuestWalk.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/main_purple</item>
        <item name="postSplashScreenTheme">@style/Theme.QuestWalk</item>
    </style>
</resources>
